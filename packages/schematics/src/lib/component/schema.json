{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsRibaComponent",
  "title": "Riba web component Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the web component.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the web component?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the web component."
    },
    "language": {
      "type": "string",
      "description": "Riba web component language (ts/js)."
    },
    "sourceRoot": {
      "type": "string",
      "description": "Riba web component source root directory."
    },
    "templateEngine": {
      "type": "string",
      "description": "The template engine you want to use for your web component (html/pug).",
      "default": "html"
    },
    "flat": {
      "default": false,
      "description": "Flag to indicate if a directory is created."
    },
    "spec": {
      "default": true,
      "description": "Specifies if a spec file is generated."
    }
  },
  "required": ["name"]
}