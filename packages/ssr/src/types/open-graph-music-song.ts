export interface OpenGraphMusicSong {
  /** integer >=1 - The same as music:album:disc but in reverse. */
  disc?: number;
  /** integer >=1 - The same as music:album:track but in reverse. */
  track?: number;
}
