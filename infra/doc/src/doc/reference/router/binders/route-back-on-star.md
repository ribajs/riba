`rv-route-back-on-*` listens to a custom event and performs `history.back()`.

```html
<button rv-route-back-on-click>Back</button>
```

This is useful when you need declarative back navigation behavior on custom UI
elements.
