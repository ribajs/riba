### Adapters

```html
<select rv-value="user.address:city">
```

Riba.js includes a built-in `.` adapter for plain JavaScript objects.
Custom adapters are supported for advanced integrations with external state
models. Most apps only need the default adapter.
