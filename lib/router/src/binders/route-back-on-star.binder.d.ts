import { IBinder, eventHandlerFunction } from '@ribajs/core';
export declare const goBack: () => void;
/**
 * Calls `window.history.back()` an event.
 */
export declare const routeBackOnStarBinder: IBinder<eventHandlerFunction>;
