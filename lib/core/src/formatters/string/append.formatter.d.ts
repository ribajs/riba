/**
 * Appends characters to a string.
 * @see https://help.shopify.com/themes/liquid/filters/string-filters#append
 */
export declare const append: {
    name: string;
    read(a: string, b: string): string;
};
