/**
 * Converts a string into JSON format.
 * @see https://help.shopify.com/themes/liquid/filters/additional-filters#json
 */
export declare const json: {
    name: string;
    read(object: any, replaceSingleQuate?: boolean): string;
};
