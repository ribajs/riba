{
  "name": "ribajs",
  "description": "Declarative data binding + templating solution.",
  "version": "1.0.1",
  "author": "Pascal Garber <pascal@jumplink.eu>",
  "contributors": [],
  "private": true,
  "url": "https://github.com/ribajs/riba",
  "license": "MIT",
  "licenses": [
    {
      "type": "MIT",
      "url": "https://github.com/ribajs/riba/blob/master/LICENSE"
    }
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ribajs/riba.git"
  },
  "scripts": {
    "link": "(cd packages/core && npm run link) && (cd packages/i18n && npm run link) && (cd packages/router && npm run link) && (cd packages/bs4 && npm run link) && (cd packages/shopify && npm run link) && (cd packages/shopify-easdk && npm run link) && (cd packages/shopify-tda && npm run link) && (cd packages/core && npm install)",
    "bump-version": "node tools/bump-version.js",
    "publish": "node tools/bump-version.js --publish",
    "doc": "harp server docs/_harp ./docs",
    "build-doc": "harp compile docs/_harp ./docs"
  },
  "devDependencies": {
    "@types/jest": "^24.0.17"
  },
  "dependencies": {}
}
